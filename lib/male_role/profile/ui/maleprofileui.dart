import 'package:flutter/material.dart';

class MaleProfileUI extends StatelessWidget {
  const MaleProfileUI({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Male Profile')),
    );
  }
}
